# wechat-author-teardown — 配置样例
# 用法：cp config.example.env config.env 后按需修改。scripts 会自动加载 config.env，
# 没有就用这些默认值。所有路径支持 ~ 展开。

# 原始归档落地根目录（每个博主一个子目录）
ARCHIVE_ROOT="$HOME/wx-archive"

# 抓取引擎：auto | browse | requests | manual
#   auto    : 优先 browse，没有则降级 requests，再不行提示 manual（见 references/fetch-engines.md）
#   browse  : gstack browse 二进制（Bun+Playwright CDP，过微信环境验证最稳）
#   requests: 纯 HTTP（仅合集 JSON 常可用；正文常撞验证页）
#   manual  : 由 agent 用自己的浏览器 MCP 取 HTML 后交给转换脚本
FETCH_ENGINE="auto"

# browse 二进制路径（FETCH_ENGINE=browse/auto 时用）。装在别处就改这里。
BROWSE_BIN="$HOME/.claude/skills/gstack/browse/dist/browse"

# 每篇之间的节流秒数（防触发验证码）
THROTTLE_SECONDS="1"

# ---- 可选：Obsidian 回流插件（默认关闭，详见 references/obsidian-integration.md）----
OBSIDIAN_ENABLED="false"
OBSIDIAN_VAULT=""                          # 例: $HOME/Library/Mobile Documents/iCloud~md~obsidian/Documents/Obsidian Vault
OBSIDIAN_TEARDOWN_DIR="内容系统/博主拆解"   # 精炼成果落地子目录（vault 内相对路径）
OBSIDIAN_FRAMEWORK_LIBRARY="内容系统/爆款框架库.md"  # 回流目标文件（agent 改前需用户确认）
